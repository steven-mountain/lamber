# Lamber 效益分析工具集 - 项目状态报告 (PROJECT_STATE)

## 1. 项目概览
本项是一个基于 **Tauri + React + Rust** 构建的售前工具集，旨在通过自动化手段提升项目效益测算、申报材料制作以及 5G/ICT 项目全生命周期管理的效率。

- **当前分支**: `feature/ai-integration`
- **当前版本**: v1.2.0 (项目看板与效益联动集成版)
- **核心架构**: 
  - 前端: Vite + React 18 + TailwindCSS + Shadcn/UI
  - 后端: Rust + Tauri (使用 native HTTP 插件规避 CORS)
  - 存储层: 通过 `ProjectRepository` 接口隔离，目前基于 `JsonProjectRepository` 读写 `projects_store.json`，并预留 SQLite 替换空间
  - AI 层: 支持 SSE 流式传输，兼容 Ollama 及 OpenAI 标准接口

---

## 2. 已完成功能 (Milestones)

### A. 效益测算模块 (Benefit Tool)
- [x] **单项目测算**: 支持 IT 集成与 CT 产品分项输入，自动计算毛利率、NPV 等。
- [x] **批量测算**: 支持 Excel 模板导入，一键生成批量计算报告。
- [x] **可视化看板**: 采用玻璃拟态设计的计算结果展示区域。

### B. 材料制作模块 (Docfill Tool)
- [x] **模板引擎**: 实现基于 Rust 的 Word 文档变量填充。
- [x] **表单系统**: 结构化的项目基本信息输入界面。

### C. Lamber 智能顾问 (AI Copilot)
- [x] **流式输出控制**: 解决 SSE 重复拼接 Bug，实现平滑打字机效果。
- [x] **上下文感知**: 自动抓取当前页面的测算数据（如毛利率、NPV）进行分析。
- [x] **混合知识体系**: 建立内置产品库，实现 `[系统内置]` 与 `【系统外扩展】` 标签的差异化渲染。
- [x] **配置持久化**: AI 设置（端点、模型、API Key）自动保存至 `localStorage`。

### D. 项目看板与效益分析联动 (Project Board & Benefit Linkage) - NEW!
- [x] **仓储数据层设计**: 采用 `ProjectRepository` 接口规范存储，数据模型划分为 `Project`、`BenefitAnalysisScheme`、`BenefitAnalysisSnapshot`，并在 JSON 存储层增加 `schemaVersion` 保证迁移安全。
- [x] **扁平瀑布流横向卡片布局**: 将看板从传统的竖向 Kanban 多列滚动重构为纵向滚动的大瀑布流；每个项目卡片采用精美横向行式布局，合理平铺项目概况与效益分析指标，提升阅读易读性与尺寸自适应性。
- [x] **看板视图切换与持久化**: 支持在横向列表与卡片/宫格视图（基于 CSS Grid 响应式布局 `minmax(320px, 1fr)`）之间一键切换，并自动将视图选择持久化存储于 `localStorage`。对于无指标的项目，提供友好文案引导。
- [x] **项目阶段进度筛选 Pills**: 顶部集成精美胶囊按钮组，支持按项目阶段进度 tag (全部、立项中、审批中、实施中、已完成) 进行一键过滤，并自带分类项目数量计数。
- [x] **自由/关联测算双模式与随时切换**: 效益测算重构为支持“自由测算”与“项目关联测算”双模。顶端集成项目关联状态横幅与切换下拉框：自由模式下可随时选择并绑定已有项目；绑定模式下支持一键断开关联、保存或另存为新方案，并可随时切换到其他项目。
- [x] **入口源头感知回退导航**: 导航 Store (`useNavigationStore`) 智能跟踪 `entrySource` 历史状态。用户从主页 Hub 或项目看板进入效益分析时，返回按钮能够智能感知并精准退回原入口，中途在测算工具中切换项目亦不会扰乱原回退路径。
- [x] **回填与逆向恢复**: 打开效益分析时，系统根据 explicit context (优先) 恢复测算，自动将最新 Snapshot 里的 `inputParams` 逐一回填表单并自动触发算账。
- [x] **保存与版本快照**: 测算后支持“保存至当前方案”和“另存为新方案”；每次保存均产生一个新的快照（含完整输入/输出），并自动写入 `ProjectLog`（如 `更新效益分析方案：毛利率 xx%`）。
- [x] **新建测算方案默认命名优化**: 新增效益方案时自动预填当前项目名称，且在效益分析保存时若无特定方案名则直接默认使用项目名称作为方案名，减少用户重复输入操作。
- [x] **后端判定风险等级**: 以后端 Rust Service 层的 `calculate_risk_level` 函数为风险等级的唯一判定标准，确保与财务规则一致。
- [x] **详情 Drawer 关闭机制优化**: 实现了全局监听 `ESC` 键盘事件以及点击 Drawer 外部区域关闭详情面板的功能，同时通过对弹窗状态的跟踪，安全规避了子级嵌套模态框（如新增方案、重命名等）操作时的误关闭。

### E. 项目本地目录与交付件管理 (Project Files & Scanning Management) - NEW!
- [x] **目录双模绑定**: 支持为每个项目“绑定本地项目目录”，记录文件夹路径；且支持直接在系统内调用资源管理器打开该目录。
- [x] **文件扫描同步引擎**: 实现 Rust 目录扫描器，递归/非递归检索 Word, Excel, PDF 等核心交付件并自动归类，标记文件的存在性 (exists) 及最新扫描时间 (lastScannedAt)。若文件在磁盘缺失则保留记录并标记红牌警告，绝不物理删除。
- [x] **文件存储双模式**: 区分 “关联本地文件 (linked)” (仅记录路径，删除时不移出磁盘) 与 “应用沙盒托管 (copied)” (物理拷贝至 sandbox 目录，删除时可二次确认安全清理)。
- [x] **项目文件管理 Tab 页重构**: 优化详情页“文件”Tab 控制栏，拆分为“本地工作目录信息”与“操作按钮行”两部分，引入 Flex 换行与间距微调，使五个以上的操作按钮在窄视口 Drawer 内自适应排列、不再重叠拥挤。
- [x] **角色标识与主文档映射**: 支持将文件一键标记为 “效益测算主文档” 或 “预算主文件”，并自动与 `Project` 身上的 `main_document_path`/`main_budget_file_path` 进行双向绑定与回写。
- [x] **表格文件解析自动导入**: 文件列表中针对 Excel 类型的效益测算表，新增“一键导入”按钮，点击后调用后端 `parse_benefit_excel` 进行单元格定位与财务公式判定，并在双重确认（是否确认覆盖、是否解绑并覆盖）安全提示后直接更新数据库中当前方案的测算指标。
- [x] **看板详情页标签集成**: 在项目详情抽屉中，通过极简无框 Tab 设计集成“项目文件管理”子模块，支持搜索过滤、系统内双击打开、操作系统 Finder 定位及删除确认。

---

## 3. 待开发计划 (Roadmap)

### 第一阶段：AI 深度集成 (Phase 2 补全)
- [ ] **多轮对话上下文优化**: 优化历史消息清理逻辑，防止 Context Window 溢出。
- [ ] **本地知识库扩展**: 允许用户上传本地 PDF/Markdown 文件作为 AI 的私有知识增强 (RAG)。

### 第二阶段：工程优化 (Engineering)
- [ ] **安装包体积优化**: 移除开发期冗余依赖。
- [ ] **多语言支持**: 引入 i18next 实现中英文切换。

---

## 4. 关键技术细节
- **AI 接口地址**: 默认 `http://localhost:11434/v1/chat/completions` (Ollama)。
- **持久化配置**: AI 配置与项目上下文 ID 存放在浏览器 `localStorage` 中。
- **共享仓储模型**: `StoreData` 是跨 `project` / `benefit` / `project_files` 的共享存储模型，存放在 `projects_store.json`，各 Repository 读写时通过原子 load-modify-save cycle 避免并发覆盖。
- **构建指令**: `npm run tauri dev` (开发) / `npm run tauri build` (打包)。

---
*最后更新时间: 2026-05-21*
