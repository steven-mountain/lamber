# “一键生成全流程文档”模板页面组织复用优化

- **Status:** Done
- **Objective:** 在不改变后端、接口、字段 key、字段映射和文档生成逻辑的前提下，将《立项签批表》和《需求导入表》扩展为与《会审纪要》一致的 Tab 分组填写体验，并抽象可复用的模板页面骨架。

## Progress

1. [x] 抽象 `TemplateDocumentLayout`、`TemplateTabSection`、`TemplateSubmoduleCard`、`TemplateConfirmationPanel` 和通用完成度/底部指标展示逻辑。
2. [x] 《会审纪要》继续使用横向 Tab 分组、完成度提示和固定底部效益评估栏，保持既有字段与生成逻辑不变。
3. [x] 《ICT项目立项签批表》改为项目基础信息、服务内容、商务条款、立项与采购、生成确认五个内容区 Tab。
4. [x] 《ICT项目需求导入表》改为需求基础信息、需求内容、商务与预算、实施与风险、生成确认五个内容区 Tab。
5. [x] 保留原字段、原 `gen_*` 表单名、原状态绑定、常用内容入口、动态清单、图片上传、AI 图片分析和文件生成函数。
6. [x] ICT 生命周期页对三类 Tab 模板隐藏外层旧表单卡片与重复底部指标面板，左侧仍只保留测算流程和模板列表导航。

## Validation

- `npm run build --prefix src-ui`

## Scope Boundary

- 未修改任何 Rust/Tauri 后端逻辑。
- 未修改接口、数据结构、字段 key、字段映射或模板生成变量。
- 未减少、合并或删除《会审纪要》《立项签批表》《需求导入表》现有字段。
- 未改变字段保存、套用常用、存为常用、AI 生成/分析、动态清单、图片上传或文件生成行为。
- 本次 UI 组织优化仅在模板名包含“会审”“立项签批表”“需求导入表”时启用。
