# 结构反算成功判定独立验收（2026-09-09）

依据 [专项任务书](../tasks/TASK_BOOK_structure_reverse_success_check.md) 完成桌面原函数修复。D 卡片未开工，本记录不代替 D 的逐科目金额前后展示及 14b 原文回执验收。

## 修复与硬约束

根因是跨目标区间的最佳候选未经达标筛选就进入解集，最后只核验自洽即写入。现在仅达标候选进入解集，并在最终重新求值后验证达标。只在达标候选间保留金额改动最小的偏好。中点命中处理不变，区间塌缩退出时保留最佳候选。

求值、校验及结果信息准备与金额提交分开，所有业务拒绝和准备阶段异常均在写入之前返回。继续用原 `updateTaxItemsInclBatch` 同步资金计划，成功提示、批量参数和原因字段保持原样。

直接从生产源码加载并断言 `METRIC_EPSILON=0.0001`、`MONEY_EPSILON=0.004`、金额舍入/格式化与修复前一致；目标 setter spy 必须 0 调用，目标绑定值也必须原样保留。三处 `modelEAmountMode=false` 保留。没有改费率、财务公式、年度资金计划同步或零容差规则。

## 任务书逐条核对

| 要求 | 实际验证 |
| --- | --- |
| 1 原始复现 | 冻结修复前生产函数＋9.4%/10.8%引擎桩，请求10%，确实调用1次金额写入且报完成；30/70变40/60，实际9.4%。修复后拒绝且0调用。 |
| 2 范围可见 | 错误包含目标10.00%、最接近9.40%、9.40%–10.80%，并注明范围内未必每个值可达。上下界外、不敏感、最终复算拒绝也验证具体数字。 |
| 3 正常路径 | 12个场景逐项比较修复前后完整成功文案、批量金额/原因、显示指标和上下文：增/减、利润率/NPV率、双解最小改动、两侧、固定同侧金额、边界和容差内命中。另1个场景验证较近未达标候选不会挤掉较远达标解。 |
| 4 塌缩 | 测试桩将候选金额压为同一点以触发防御性塌缩，完整函数拒绝且报9.4%；独立执行生产二分循环，直接断言best仍为9.4%，最后中点为10.8%。未改变生产采样或容差，不把此注入场景称为真实财务案例。 |
| 5 边界 | 目标恰好9.4%/10.8%成功；目标金额归零/等于整个池也成功，与修复前逐项一致。 |
| 6 写入顺序 | **35条失败逐个使用 `node:test` 的mock函数断言 `updateTaxItemsInclBatch.mock.callCount() === 0`**，金额/目标不变且不发布结果或修改忽略标志。成功只调用1次，所有求值及上下文准备在此之前。 |
| 7 回归 | 下述前端、反算、资金计划和Rust回归均通过。 |

35条拒绝覆盖：入口目标/科目/承接规则；相同目标承接、负池；样本无效/缺载荷/空结果；候选构造及样本引擎异常；不敏感、超上下界；利润率/NPV率两方向跳变；搜索无效/缺载荷/空结果/引擎异常；最终无效/缺载荷/空结果/异常、总额变化、两科目各自负值、最终指标漂移及刚超容差；上下文准备异常；容差外近似值及塌缩拒绝。停用的 model_e 分支不假称可达或已执行。

## 运行命令与证据

- `cd src-ui && npm run test:structure-reverse`：35个失败、13个成功；[完整调用次数、原错误和金额结果](./structure-reverse-success-check-evidence.json)。测试每次从当前生产函数提取代码，不复制修复实现；[修复前固定源码](../../src-ui/scripts/fixtures/structure-reverse-before-success-check.json)仅用于复现与成功对照。
- `cd src-ui && npm run lint && npm run build`：通过；Vite保留既有大chunk提示。
- `test_ict_reverse_search.cjs`，`test_subject_funding_{plan,sync,proportional,cashflow,migration,phase4,final}.cjs`：全部通过。
- `test_funding_batch_zero.cjs`：生产甄选/结构反算调用参数→生产批量setter，正值与旧逻辑一致、清零→序列化/重载→恢复原四年计划通过。
- `test_benefit_simulation.cjs`：168组旧桌面/共享编辑差分、28科目与年度数组对照通过；未重新生成基线。
- `test_selection_fee.cjs`、`test_tax_split.cjs`、`test_selection_result_batch.cjs`、`test_chat_document.cjs`：通过。
- `cargo test --manifest-path src-tauri/Cargo.toml`：100通过、0失败、22显式ignored未运行；保留既有编译警告。

本次引擎、候选与提交为受控测试桩，不是用真实Rust项目制造9.4%/10.8%分段曲线，也没有真实项目写入。此前临时开工证据显示10.8%，本次直接加载生产常量并冻结原函数的可重跑证据显示9.4%；两者都证明未达目标仍调用写入，本轮精确对照以本次固定源码和脚本为准。此次没有重做桌面点击、真实模型或Windows发版验收；D继续单独一轮。
