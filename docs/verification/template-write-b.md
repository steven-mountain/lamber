# 阶段 B：模板文本写入验证

日期：2026-09-07。用户明确指示“那可以开始阶段B了”，据此继续实施。没有把先前自动化演练改记为 Gate A 真人验收。

## 已验证

- 常规 Rust：87 通过、18 ignored；真实 Key ignored 套件：18 通过。包含真实模型调用填字段、审批修改与最终保存一致。
- 后端边界：其他项目、通用聊天、缺失身份/绑定、金额/税率/年限/折现率/结果、清单/图片/非文本/未知字段均拒绝；拒绝与超时不写，授权只消费一次。
- 一致性：旧值来自保存态，修改不得替换项目/模板/字段集合；版本冲突、撤销绑定不写；旧 UI 保存不能覆盖新批准内容。模型原值与修改后值共同记录在原审批表。
- 前端 lint/build、dsh/template-state/session-scope/approval-review 四组回归通过；插件 typecheck、限权、HTTP 契约检查通过；打包脚本 6 项通过。
- 独立 macOS debug app 完整构建，实际应用二进制与插件契约 v3 校验通过。

## Computer Use 产品链路

使用独立 `Lamber Scope Test`、合成工作区及“3A流式验收项目”，没有写正式工作区或更换正式安装应用。

1. 模板页清空服务内容，完成度从 11/11 降到 10/11。
2. 产品聊天真实模型提交 327 字、三段服务正文；弹窗显示实际项目/模板/旧值为空。
3. 在审批框修改回退方案为切换前演练和保留原网络，批准后保存 337 字；返回模板页完成度为 11/11。
4. 发现并修复浮窗批准后主窗残留相同弹窗：Tauri 前端默认 Any 订阅会收到定向事件。改为当前窗口目标订阅，并广播审批完成清理队列。重新构建后第二次真实聊天审批修改“部署环境要求”，返回主窗确认无重复弹窗且新值同步。
5. 在“生成确认”页实际生成需求导入表 DOCX。只读核对 Word XML：两次人工编辑文本均与保存态、审计 approvedArgs 一致；原两张附件图片仍嵌入。

证据：[真实模型](./template-write-b-real-evidence.json)、[产品界面与文档核对](./template-write-b-ui-evidence.json)。证据中“人工修改”是 Computer Use 操作审批控件，不能据此声称用户真人 gate 通过。

## 待用户验收及限制

**Gate B 真人仍待完成。** 在独立测试应用选需求导入表，将一个文本字段留空；聊天让 AI 填入需求正文，在审批框读新旧对照并改一处，批准后返回模板确认完成度 +1，再生成 Word 核对改文。自动化不能替代可读性判断。

仅支持共享目录的需求导入表 8 个文本字段；其他模板、列表及图片不在本工具范围。Windows NSIS 安装及此前剩余真人发版 gate 未执行，不能当作正式发布已通过。前端构建仍有已有的大 chunk 提示，Rust 有既有编译警告；均未阻断构建。
