# D 智能结构反算开工核对：暂停（2026-09-09）

用户要求 D 单独开发验收，结果逐科目展示变更前后，三条可达范围提示不可压缩；任务书与代码不一致先停。本轮只核查、执行隔离控制流复现及记录，未修改业务代码、卡片或真实项目。

## 1. 任务书所列 model_e 路径已停用

任务书关键事实4、D16及验收14仍要求分板块金额计划联动与映射失败；现行 `useIctCalculations.ts:788`、`:856` 的 `modelEAmountMode` 均为常量 false：

- 候选构造跳过 `applyModelEStructureTransfer`，segments 原样返回、transfers 空数组。
- 实际执行 `:822` 的 `syncSubjectFundingPlansToAmounts`，通过 `fundingPlansOverride` 重建科目收付款计划年度现金流。架构文档4.3.1也明确科目计划为正式现金流唯一来源。
- 因此 `:534`、`:643` 的 model_e 映射失败及 `:863`、`:908` 对应分支无法沿当前正式路径按任务书构造；`:994` 不写分板块计划。

建议按现行科目收付款计划修正 D 的展示/回归口径，并如实注明旧分板块计划未参与本次变更，不能为通过验收重新启用遗留 model_e 路径。

## 2. :969 未承担其文案宣称的“无稳定收敛”判定

`:917` 不敏感与 `:921` 超范围的原提示、具体范围数字均存在，可按原路径带回。`:969` 却只判断 `solutions.length === 0`：

1. 有样本达到目标容差即加入 solutions（`:924–929`）。
2. 有相邻样本跨越目标时运行二分，但无论中途因无效点退出、区间收窄或次数耗尽，`:965` 都无条件加入 best。
3. 后面只检查最终候选有效、同侧总额与非负，**没有再次检查最终指标是否达到目标容差**，随后写金额并弹完成提示。

对正常有限样本，目标通过范围检查后，如果没有命中样本，就会存在跨越目标的相邻点并加入 best。因此无法把正常“不收敛”场景送到 :969；浮点容差边界或伪造内部状态不能当作该条验收。

### 隔离复现

用 TypeScript AST 原样提取生产 `performLockedTotalStructureReverseCalculation`，候选构造与引擎替换为测试桩，保留生产采样函数和搜索/成功判定不变。测试桩指标仅取 0.094 / 0.108，请求 0.1：

- 执行22次评估后调用测试桩的金额写入接口（两科目40/60→50/50）。
- 返回“结构反算完成…目标10.00%，当前10.80%”。
- :969 未出现。

[完整控制流证据](./ai-structure-reverse-preflight-witness.json)。这是控制流反例，**不是声称真实 Rust 引擎/项目必然产生此分段指标**，没有数据库或 UI 写入。

## 待确认

任务书 D15 要求“不复制、不重写二分搜索与承接规则的任何一行”，又要求验收14b实际触发 :969；目前无法同时满足。建议先明确：

1. D 以现行科目收付款计划为准，而非恢复旧分板块金额同步。
2. 是否另行授权修正反算的成功/收敛判定，使未达目标不写金额、带回原始可达区间，再接入结果卡片。

未自行决定新容差、新搜索策略或近似解是否可接受。D尚未实现/验收，A/B/C已完成状态保持独立。
