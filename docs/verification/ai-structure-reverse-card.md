# D：智能结构反算卡片独立验证

日期：2026-09-09。范围仅 D；A/B/C、达标前置修复、提交归一前置修复分别验收。

## 交付

用户在聊天卡片明确选择目标科目和指标，先读取当前原编辑器的可达范围，再确认目标。缺目标留空，换页/重新邀请不复用科目；否定、历史和税率数字不作为目标。没有新增 AI 金额写入/结构反算工具，没有复制搜索、修改费率或自动降低目标。

原桌面 `alert` 文案保留；卡片使用同一函数的返回结果，全部错误原样进入应用回执及下一轮上下文。四条范围错误包含目标、最接近值、采样范围，最终复算不一致时保留其实际值。目标/实际并排四位百分数，卡片独立核对原容差；金额及科目收付款计划逐项前后对照，全部 10 年尾差保留。

两项前置已独立完成：

- [达标后才写入](./structure-reverse-success-check.md)
- [校验值与真实写入值一致](./structure-commit-normalization.md)

## 原桌面逐项对照（真实 Tauri + Rust 引擎）

使用独立 Scope Test 应用与合成 28 科目项目。正式项目和正式应用配置未改。所有业务写入均由 Computer Use 点击原产品控件及保存按钮；SQLite 仅以 `mode=ro` 读取验证。

1. 从集市打开已绑定聊天，未先打开测算页。明确请求结构反算但未给目标；卡片科目为空，目标输入尚未显示。用户选择投入设备后预览自动打开对应甄选前方案。
2. 原快照 v7，目标科目为“主要设备/甲供材料（开票名称10）”，承接为“集成服务（开票名称13）”，锁定投入总额 **89888.00**，采样范围 **19.13%–22.16%**。范围出现后目标仍为空。
3. 用户填 **12%** 并确认：原错误完整出现在卡片及聊天。下一轮只询问失败原因，真实模型完整复制原错误，未调用测算/写工具。

```text
当前锁定总额结构下无法达到目标值。目标毛利润率：目标 12.00%，本次搜索最接近值 19.13%；当前结构下可达范围约为 19.13% - 22.16%（采样范围不代表其中每个值都能达到）。
```

4. 重新邀请，科目必须重选。用户确认 **20%** 后，卡片和应用回执目标/实际均为 **20.0000%**。原产品保存得到 v8。

| 科目 | 变更前含税元 | 变更后含税元 | 第1—3年付款（每年） | 第4年付款 |
| --- | ---: | ---: | ---: | ---: |
| 主要设备/甲供材料 | 1060.00 | 15542.25 | 265.00 → 3885.56 | 265.00 → 3885.57 |
| 集成服务（承接） | 53000.00 | 38517.75 | 13250.00 → 9629.44 | 13250.00 → 9629.43 |

第5—10年均 0.00 → 0.00，卡片和模型均完整列出。固定同侧合计 **35828.00**，可重分配池 **54060.00**，总额仍 **89888.00**。

5. 通过原金额输入恢复 1060/53000 并保存 v9，28 科目、有效年度计划、四组现金流和全部指标与 v7 相同。恢复过程的计划历史字段如实保留，未篡改为旧历史。
6. 从同一基准点击原桌面“智能反算”按钮，目标 0.2，保存 v10。**v8 与 v10 的完整输入及输出逐字段相同，仅 4 个计划时间戳不同**：28/28 科目（含名称、税率、拆分）、全部计划字段/10年数组、四组现金流、全部指标及逐年输出均精确一致。

| 指标 | 卡片保存 v8 | 原桌面保存 v10 |
| --- | ---: | ---: |
| NPV | 18280.51 | 18280.51 |
| NPV率 | 0.2500 | 0.2500 |
| 毛利润率 | 0.2000 | 0.2000 |
| 动态回收期 | 1 | 1 |
| IT NPV | 34871.29 | 34871.29 |
| IT NPV率 | 0.6402 | 0.6402 |
| IT毛利润率 | 0.3903 | 0.3903 |
| IRR | --（未计算） | --（未计算） |

7. **D16b 不同值实测**：用户明确目标20.005%，卡片显示 **目标20.0050% / 实际20.0000%**，原桌面目标字段仍为 **0.20005**。差0.00005，在原0.0001容差内；未改目标。模型下一轮原样并列两个四位百分数，未再反算。
8. 最终恢复合成基准并保存 v11，28科目、有效年度计划及指标与 v7 相同。独立应用已退出，临时 AI 配置已恢复并核对。

[逐项保存证据（含全部输入/输出）](./ai-structure-reverse-desktop-evidence.json)。界面控件、表格和文本通过原生 accessibility 检查；未保留可用的像素截图，不将此项声称为完整视觉截图验收。

## 自动验证

- `test_structure_reverse_success.cjs`：两种自动修正设置各 **35 条失败**，逐条断言 `updateTaxItemsInclBatch` spy **0 调用**，原目标/金额/计划未变；原桌面 alert、卡片返回值与冻结卡片前函数错误精确一致。13条成功及原搜索策略回归通过。
- `test_structure_commit.cjs`：冻结原100.01缺陷可复跑；有效候选和真实批量提交全量相等；归一后目标失败/总额无法闭合均零调用；**672** 组原批量语义对照、CT两条联动/两种开关通过。
- `test_chat_structure_reverse.cjs`：四条原错误分别跑原函数控制流和卡片 IPC → 聊天 → appReceipt，完整字符串一致，每条spy为0。覆盖可信绑定、未绑定/通用不显示、选择器失败不回退、预览一次性/输入变化失效、目标来源、28科目遍历及10年尾差。
- 反算、7份资金计划、批量清零、168组假设试算差分、税额、甄选费/批量、聊天文档、会话作用域、会审清单回归通过。
- 前端 lint/build、插件 typecheck、6项打包脚本检查通过；独立 macOS debug 应用重建。
- Rust 常规 **100通过 / 23 ignored**。完整真实集成首跑 **22通过 / 1失败**；失败为既有模板目录审批用例未写入预期正文，同一测试与断言未改，单独重跑通过。不能描述为首跑23全过。
- D 真实模型专项本轮独立运行和完整组运行均通过：四条原错误完整引用，无自行改目标/编金额；成功目标/实际、计划尾差保留。[模型原答证据](./ai-structure-reverse-real-model-evidence.json)。

## 证据边界

- 四条范围错误的穷举和零调用证明使用生产控制流、受控指标引擎及测试桩；真实桌面/Rust额外覆盖正常、超范围、保存对照与容差内差异。没有声称通过真实UI自然构造所有不收敛/最终漂移场景。
- 本轮真实桌面使用自动修正关闭；开启分支由有效提交专项、672组批量对照及两套35条失败覆盖。
- 中途强制关闭窗口/切换工作区、断网和进程崩溃未作桌面故障注入；现有写前绑定/输入校验及一次性令牌自动测试不能等同于这些操作的端到端验收。
- 合成数据与自动/Computer Use验收不替代既有真人审批、Windows签名/发布gate。未提交、未发布。
